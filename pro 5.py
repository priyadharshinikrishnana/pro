vv,pp=map(int,input().split())
tab=[]
cine=[]
priya=[int(o) for o in input().split() ]
for k in range(0,pp):
    u1,v1=map(int,input().split())
    for l in range(u1-1 ,v1):
        sine.append(priya[l])
    kah=sorted(cine)
    tab.append(min(cine))
    del sine[:]
for p in range(0,len(cab)):
    print(tab[p])
